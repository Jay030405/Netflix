pract1

mkdir cm
cd cm
git init

echo "hello">file1.txt
git add file1.txt
git --version

git config --global user.name "Aryan"
git config --global user.email "aryan@gmail.com"
git config --list

git init
git status

git add <file_name>
git add .

git commit -m "commit message"

git log
git log --stat
git log --pretty=oneline
git log --pretty=format:"%h %an %ar %s"
git log --name-status

git log --since="2 weeks ago"
git log -1
git log --before="2025-12-10 22:00:00"
git log --after="2025-12-10"

git log --author="Aryan"
git log --author="Aryan" --since="7 days ago"

git log --pretty=fuller

pract2

git reset HEAD <filename>
git restore --staged <filename>
git restore <filename>

git clone <repository_url>

git remote
git remote -v
git remote add <name> <url>
git remote rename <old_name> <new_name>
git remote show origin

git fetch origin
git push origin main

git tag
git tag v1.0
git tag -a v1.0 -m "message"
git tag v1.0 <commit_id>
git tag -v v1.4.2
git push origin v1.0
git push --tags

git config --global alias.ci commit

pract3

git branch
git branch <branch-name>
git branch -m <new-name>
git branch -d <branch-name>
git branch -D <branch-name>
git branch -a

git checkout <branch-name>
git checkout -b <branch-name>

git switch <branch-name>
git switch -c <branch-name>

git merge <branch-name>

git log --oneline --graph --all
git show <branch-name>

git push origin <branch-name>
git push -u origin <branch-name>

git pull origin <branch-name>

git push origin --delete <branch-name>


pract8

mvn archetype:generate

maven-archetype-quickstart

3

groupId: com.college
artifactId: MavenDemo
version: 1.0-SNAPSHOT
package: com.college
 Y: Y

cd MavenDemo

mvn compile

mvn package

java -cp target/MavenDemo-1.0-SNAPSHOT.jar com.college.App

pract9

Your WAR file name is:

MyWebApp.war

Instead of:

MyWebApp-1.0-SNAPSHOT.war

This just means your pom.xml version is different, so Maven created a shorter name. Both are valid.

What you should do now
1️⃣ Copy the WAR file

Copy:

MyWebApp.war

from:

C:\Users\Ayan\prac9\MyWebApp\target
2️⃣ Paste it in Tomcat

Paste it into:

C:\tomcat\apache-tomcat-9.0.115\webapps
3️⃣ Restart Tomcat

Run:

cd C:\tomcat\apache-tomcat-9.0.115\bin
shutdown.bat
startup.bat
4️⃣ Open your web app

Open browser:

http://localhost:8080/MyWebApp

You should see:

Hello World!

from your index.jsp.
hello
